{
  "id": "email-ingestion",
  "name": "IMAP Email Ingestion",
  "nodes": [
    {
      "parameters": {
        "onEmailReceived": "nothing",
        "options": {
          "downloadAttachments": false,
          "forceUtf8": true
        }
      },
      "id": "c61b9a9d-b4b3-4674-8b65-0a149b552fa1",
      "name": "IMAP Email Trigger",
      "type": "n8n-nodes-base.emailReadImap",
      "typeVersion": 2,
      "position": [
        250,
        240
      ],
      "credentials": {
        "imap": {
          "id": "vocalassist-imap-creds",
          "name": "VocalAssist IMAP Support Email"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "const email = $json;\n\n// Extract sender email cleanly\nlet senderEmail = \"\";\nif (email.from && email.from.value && email.from.value.length > 0) {\n  senderEmail = email.from.value[0].address || \"\";\n} else if (typeof email.from === 'string') {\n  senderEmail = email.from;\n}\n\n// Handle body parsing (plain text and HTML emails)\nlet parsedBody = \"\";\nif (email.text) {\n  parsedBody = email.text.trim();\n} else if (email.html) {\n  // Pre-process common HTML block tags to preserve formatting and readability\n  let cleanHtml = email.html\n    .replace(/<style([\\s\\S]*?)<\\/style>/gi, '')   // Remove CSS styles\n    .replace(/<script([\\s\\S]*?)<\\/script>/gi, '') // Remove JS scripts\n    .replace(/<\\/p>/gi, '\\n\\n')                  // Convert paragraphs to double newlines\n    .replace(/<br\\s*\\/?>/gi, '\\n')                 // Convert breaks to newlines\n    .replace(/<\\/div>/gi, '\\n')                   // Convert div ends to newlines\n    .replace(/<\\/tr>/gi, '\\n')                    // Convert table row ends to newlines\n    .replace(/<\\/li>/gi, '\\n');                   // Convert list item ends to newlines\n\n  // Strip all remaining HTML tags\n  parsedBody = cleanHtml\n    .replace(/<[^>]*>/g, '')\n    .replace(/&nbsp;/gi, ' ')\n    .replace(/&amp;/gi, '&')\n    .replace(/&lt;/gi, '<')\n    .replace(/&gt;/gi, '>')\n    .replace(/&quot;/gi, '\"')\n    .replace(/\\n\\s*\\n+/g, '\\n\\n')                // Normalize multiple newlines\n    .replace(/[ \\t]+/g, ' ')                      // Collapse multiple horizontal spaces\n    .trim();\n}\n\nreturn {\n  json: {\n    ticketId: email.messageId || email.uid || `email-${Date.now()}`,\n    source: \"Email\",\n    sender: senderEmail,\n    subject: email.subject || \"[No Subject]\",\n    body: parsedBody,\n    receivedAt: email.date || new Date().toISOString()\n  }\n};"
      },
      "id": "a18413b5-31ad-4cc2-9844-42b7a95015ff",
      "name": "Parse Email Content",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        470,
        240
      ]
    }
  ],
  "connections": {
    "IMAP Email Trigger": {
      "main": [
        [
          {
            "node": "Parse Email Content",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {},
  "versionId": "ef056dbf-fdf3-4c91-9e75-9c9f69747a83",
  "meta": {
    "templateCredsSetupCompleted": true
  },
  "tags": []
}

